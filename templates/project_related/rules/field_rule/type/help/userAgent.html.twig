{% trans_default_domain 'mosparo' %}

<p>{{ 'rules.fieldRule.type.userAgent.intro'|trans }}</p>

<ul>
    <li>
        <strong>{{ 'rules.fieldRule.type.userAgent.text.title'|trans }}</strong>
        <p>
            {{
                'rules.fieldRule.type.userAgent.text.intro'|trans({
                    '%star%': '<code>*</code>',
                    '%value%': '<code>*value*</code>'
                })|raw
            }}
        </p>
        <p>
            <u>{{ 'general.example'|trans }}:</u> <code>*curl*</code>
        </p>
    </li>
    <li>
        <strong>{{ 'rules.fieldRule.type.userAgent.regex.title'|trans }}</strong>
        <p>
            {{ 'rules.fieldRule.type.userAgent.regex.intro'|trans }}
        </p>
        <p>
            <u>{{ 'general.example'|trans }}:</u> <code>/(curl|wget)/is</code>
        </p>
    </li>
</ul>

{% include 'project_related/rules/field_rule/type/help/_rating.html.twig' %}
