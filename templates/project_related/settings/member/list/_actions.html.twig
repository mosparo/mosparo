<div class="d-flex">
    <a href="{{ path('settings_member_edit', { 'id': row.id}) }}" class="btn btn-primary me-2">
        <i class="ti ti-pencil"></i>
        Edit
    </a>
    <a href="{{ path('settings_member_remove', { 'id': row.id}) }}" class="btn btn-danger">
        <i class="ti ti-circle-minus"></i>
        Remove
    </a>
</div>