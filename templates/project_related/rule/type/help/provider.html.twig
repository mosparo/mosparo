{% trans_default_domain 'mosparo' %}

<p>{{ 'rule.type.provider.intro'|trans }}</p>

{% if is_granted('ROLE_ADMIN') %}
    <p>
        <strong>
            {{ 'rule.type.provider.infoGeoip2'|trans }}
        </strong>
    </p>

    <a href="{{ path('administration_geoip2_settings') }}" class="btn btn-secondary mb-3">
        <i class="ti ti-map-pin"></i>
        {{ 'rule.type.provider.action.configureGeoip2'|trans }}
    </a>
{% endif %}

<ul>
    <li>
        <strong>{{ 'rule.type.provider.asNumber.title'|trans }}</strong>
        <p>{{ 'rule.type.provider.asNumber.intro'|trans }}</p>
        <p>
            <u>{{ 'general.example'|trans }}:</u> <code>123456</code>
        </p>
    </li>
    <li>
        <strong>{{ 'rule.type.provider.country.title'|trans }}</strong>
        <p>{{ 'rule.type.provider.country.intro'|trans }}</p>
        <p>
            <u>{{ 'general.example'|trans }}:</u> <code>CH</code>
        </p>
        <p>
            <strong>
                {{ 'rule.type.provider.country.infoCountryBlock'|trans }}
            </strong>
        </p>
    </li>
</ul>

{% include 'project_related/rule/type/help/_rating.html.twig' %}
