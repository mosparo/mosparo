{% extends 'small.html.twig' %}

{% trans_default_domain 'mosparo' %}

{% block documentTitle %}{{ 'administration.update.finalized.title'|trans }} - {{ parent() }}{% endblock %}

{% block small_body %}
    <h2 class="card-title text-center mb-4">{{ 'administration.update.finalized.title'|trans }}</h2>

    <p>
        {{ 'administration.update.finalized.description'|trans }}
    </p>

    {% if showHostsAlert %}
        <div class="alert alert-danger mt-4">
            <div class="alert-title">
                {{ 'administration.update.finalized.hostsAlert.title'|trans }}
            </div>
            <p class="text-muted">
                {{ 'administration.update.finalized.hostsAlert.intro'|trans }}
            </p>
            <p class="text-muted">
                <strong>{{ 'administration.update.finalized.hostsAlert.intro2'|trans }}</strong>
            </p>
            <div class="d-flex justify-content-end">
                <a href="https://documentation.mosparo.io/docs/usage/settings/#hosts" target="_blank" class="btn btn-info">
                    <i class="ti ti-question-mark"></i>
                    {{ 'action.help'|trans }}
                </a>
            </div>
        </div>
    {% endif %}

    {% if showGeoIp2Info %}
        <div class="alert alert-info mt-4">
            <div class="alert-title">
                {{ 'administration.update.finalized.geoIp2Info.title'|trans }}
            </div>
            <p class="text-muted">
                {{ 'administration.update.finalized.geoIp2Info.intro'|trans }}
            </p>
            <p class="text-muted">
                <strong>{{ 'administration.update.finalized.geoIp2Info.intro2'|trans }}</strong>
            </p>
            <div class="d-flex justify-content-end">
                <a href="https://documentation.mosparo.io/docs/administration/geoip" target="_blank" class="btn btn-info">
                    <i class="ti ti-question-mark"></i>
                    {{ 'action.help'|trans }}
                </a>
            </div>
        </div>
    {% endif %}

    <div class="form-footer">
        <a href="{{ path('dashboard') }}" class="btn btn-primary w-100">
            {{ 'setup.buttons.continue'|trans }}
            <i class="ti ti-icon-right ti-chevron-right"></i>
        </a>
    </div>
{% endblock %}
