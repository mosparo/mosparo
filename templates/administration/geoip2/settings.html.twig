{% extends 'administration/administration_base.html.twig' %}

{% block documentTitle %}GeoIP2 - {{ parent() }}{% endblock %}
{% block pageTitle %}GeoIP2{% endblock %}

{% block pageBody %}
    <div class="container-xl">
        <div class="row">
            <div class="col-12 col-lg-6">
                {{ form_start(form) }}
                    <div class="card">
                        <div class="card-header">
                            <h3 class="card-title">Settings</h3>
                        </div>
                        <div class="card-body">
                            <p>
                                To get a license key, you need a MaxMind account. Please visit the following site:
                                <a href="https://dev.maxmind.com/geoip/geolite2-free-geolocation-data" target="_blank">
                                    https://dev.maxmind.com/geoip/geolite2-free-geolocation-data
                                </a> <i class="ti ti-external-link"></i>
                            </p>
                            {{ form_row(form.geoipActive) }}
                            {{ form_row(form.geoipLicenseKey) }}
                        </div>
                    </div>
                    <div class="d-flex mt-3">
                        <div class="col-auto ms-auto">
                            <button type="submit" class="btn btn-primary">
                                <i class="ti ti-device-floppy"></i>
                                Save
                            </button>
                        </div>
                    </div>
                {{ form_end(form) }}
            </div>
            <div class="col-12 col-lg-6">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Download & automatic update</h3>
                    </div>
                    <div class="card-body">
                        <p>
                            After you've saved the license key, mosparo needs to download the database. You can do that here
                            directly or you can use a shell connection to execute it with the CLI command.
                        </p>

                        <h4>
                            Web
                        </h4>
                        <p>
                            Use the following button to let mosparo download the database:
                        </p>

                        <div class="d-flex mb-3">
                            <a href="{{ path('administration_geoip2_download') }}" class="btn btn-orange mx-auto">
                                <i class="ti ti-download"></i>
                                Download the Database
                            </a>
                        </div>

                        <h4>
                            Shell
                        </h4>
                        <p>
                            Use the following command to download the database via shell:
                        </p>
                        <code class="d-inline-block mb-3">
                            php bin/console mosparo:geoip2:download-database
                        </code>

                        <h4>Automatic update</h4>
                        <p>
                            To automatically refresh the database, it is recommend to set up a cronjob which will automatically
                            download the database. To use the automatic update, please set up a cronjob like the following:
                        </p>
                        <code class="d-inline-block mb-3">
                            {{ random(0, 59) }} {{ random(0, 23) }} * * {{ random(0, 6) }} php bin/console mosparo:geoip2:download-database
                        </code>
                        <p>
                            You maybe have to adjust the path of the <code>bin/console</code> file. It could be possible
                            that you have to fill in the absolute path.
                        </p>

                        <div class="alert alert-info">
                            Please remember that the number of downloads a license key can make is limited. Please do not use the download functionality to often.
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}